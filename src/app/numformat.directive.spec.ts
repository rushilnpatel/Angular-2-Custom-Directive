import { NumformatDirective } from './numformat.directive';

describe('NumformatDirective', () => {
  it('should create an instance', () => {
    const directive = new NumformatDirective();
    expect(directive).toBeTruthy();
  });
});
